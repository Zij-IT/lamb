{
  "name": "tree-sitter-lamb",
  "version": "1.0.0",
  "description": "This is the grammar of the language Lamb written for tree-sitter",
  "main": "bindings/node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nan": "^2.17.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.20.7"
  },
  "tree-sitter": [
    {
      "scope": "source.lamb",
      "file-types": [ "lb", "lamb"],
      "highlights": [ "queries/highlights.scm" ],
      "injection-regex": "^(lb|lamb)$"
    }
  ]
}
